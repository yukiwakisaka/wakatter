<%= form_for(current_user.wakaru_relations.find_by(wakarareru_post_id: feed_item.id),
             html: { method: :delete }) do |f| %>
	<%= f.submit "わかる", class: "btn btn-xs btn-success btn-wakaru"  %>
<% end %>