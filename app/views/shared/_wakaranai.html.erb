<%= form_for(current_user.wakaru_relations.find_by(wakarareru_post_id: @item.id),
             html: { method: :delete }, remote: true) do |f| %>
  <%= f.submit "わからない", class: "btn btn-large btn-primary"  %>
<% end %>