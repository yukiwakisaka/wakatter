<%= form_for(current_user.wakaran_rels.find_by(wakararen_post_id: feed_item.id),
             html: { method: :delete }, remote: false) do |f| %>
  <%= f.submit "わからん", class: "btn btn-xs btn-danger btn-wakaru"  %>
<% end %>