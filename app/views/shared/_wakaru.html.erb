<%= form_for(current_user.wakaru_relations.build(wakarareru_post_id: @item.id), remote: false) do |f| %>
  <div><%= f.hidden_field :wakarareru_post_id %></div>
  <%= f.submit "わかる", class: "btn btn-large btn-primary" %>
<% end %>
